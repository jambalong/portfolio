---
layout: default
---

<article class="post-container">
  <header class="post-header">
    <h1>{{ data.title }}</h1>
    <time datetime="{{ data.date | date: "%Y-%m-%d" }}">{{ data.date | date: "%B %d, %Y" }}</time>
  </header>

  <div class="post-body">
    {{ content }}
  </div>
</article>